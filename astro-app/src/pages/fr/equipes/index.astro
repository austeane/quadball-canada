---
import Layout from "../../../layouts/Layout.astro";
import { getTeams } from "../../../utils/sanity";
import TeamSections from "../../../components/team/TeamSections.astro";

const teams = await getTeams('fr');
---

<Layout title="Equipes" alternate={{ en: "/teams/", fr: "/fr/equipes/" }} locale="fr">
  <section class="teams">
    <header class="teams__intro">
      <h1>Équipes</h1>
      <p>
        Les communautés de quadball à travers le Canada proposent des expériences à la fois en
        contact complet et récréatives. Parcourez les équipes ci-dessous pour trouver celle qui vous
        convient et rejoignez les responsables ou les réseaux sociaux de votre région.
      </p>
    </header>

    <div class="teams__levels">
      <h2>Niveaux de jeu</h2>
      <p>
        Le quadball au Canada comprend plusieurs parcours pour les joueurs, les bénévoles et les
        partisans.
      </p>
      <div class="teams__levels-grid">
        <div class="teams__levels-card">
          <h3>Jeunesse</h3>
          <p>
            Programmes adaptés aux jeunes pour développer leurs compétences, le travail d'équipe et
            l'amour du sport.
          </p>
        </div>
        <div class="teams__levels-card">
          <h3>Récréatif</h3>
          <p>
            Jeu amical axé sur le plaisir, l'apprentissage et la communauté. Idéal pour les personnes
            qui découvrent le sport.
          </p>
        </div>
        <div class="teams__levels-card">
          <h3>Compétitif</h3>
          <p>
            Niveau de jeu élevé pour les équipes qui participent aux championnats régionaux et
            nationaux avec un accent sur la performance.
          </p>
        </div>
        <div class="teams__levels-card">
          <h3>Équipe nationale</h3>
          <p>
            Représente le Canada sur la scène internationale et réunit les meilleurs talents du pays.
          </p>
        </div>
      </div>
    </div>

    <TeamSections teams={teams} locale="fr" />
  </section>
</Layout>

<style>
  .teams {
    padding: 3rem 0;
    display: grid;
    gap: 3rem;
  }

  .teams__intro {
    display: grid;
    gap: 1rem;
    max-width: 720px;
  }

  .teams__intro h1 {
    margin: 0;
  }

  .teams__levels {
    padding: 2.5rem;
    border-radius: 1.5rem;
    background: linear-gradient(135deg, rgba(228, 31, 122, 0.12), rgba(32, 75, 165, 0.12));
    display: grid;
    gap: 1.5rem;
  }

  .teams__levels h2 {
    margin: 0;
  }

  .teams__levels-grid {
    display: grid;
    gap: 1rem;
  }

  @media (min-width: 640px) {
    .teams__levels-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (min-width: 960px) {
    .teams__levels-grid {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }
  }

  .teams__levels-card {
    padding: 1.25rem;
    border-radius: 1rem;
    background: rgba(255, 255, 255, 0.9);
    border: 1px solid rgba(15, 23, 42, 0.08);
    box-shadow: 0 6px 18px rgba(15, 23, 42, 0.05);
    display: grid;
    gap: 0.75rem;
  }

  .teams__levels-card h3 {
    margin: 0;
    color: var(--brand-primary);
  }

  .teams__levels-card p {
    margin: 0;
    font-size: 0.95rem;
    color: rgba(15, 23, 42, 0.75);
  }
</style>
