name = "quadball-canada-auto-translate"
main = "src/index.js"
compatibility_date = "2024-01-01"

[vars]
SANITY_PROJECT_ID = "kbufa3g3"
SANITY_DATASET = "production"

# Secrets (set via `wrangler secret put`):
# - SANITY_AUTH_TOKEN
# - OPENAI_API_KEY
# - WEBHOOK_SECRET (optional, for verifying Sanity webhooks)
